# Wave 0 + Wave 1 — progress ledger

Started 2026-08-17. Controller: Opus 5. Survives compaction — **trust this file
over recollection**, and never re-dispatch a task marked ✅.

Kept in `.brain/` rather than `docs/` on purpose: `docs/` is scanned by
preflight, and a working ledger is not a specification.

## Constraints that shape the schedule

- **One port, one e2e database.** `playwright.config.js` binds :3100 with
  `reuseExistingServer: false`, and the suite seeds a single `prisma/e2e.db`.
  Only one lane may run e2e at a time — this is why Lane T runs alone.
- **Worktree isolation is unavailable in practice.** A fresh git worktree has no
  `node_modules`, so an agent there cannot run the tests that would prove its
  work. Lanes therefore share the tree and are separated by *file*, not by
  checkout.
- **Id allocation cannot be parallel.** Two sessions already collided on
  ADR-020; the duplicate-id guard exists because of it. All new FR/SDD ids are
  allocated by the controller in one commit before any lane needs them.
- **`docs/.viewer-fixture-baseline.json` is a single shared surface.** Three
  repayments in three lanes would all shrink it. One agent owns all three.

## Wave 0 — allocate before anyone writes

| Task | Status | Agent | Output |
|---|---|---|---|
| W0-S1 survey: `(pm)` top-level pages (11) | ✅ done | sonnet, read-only | `.brain/waves/w0-s1-report.md` — 10 EXISTING, **1 NONE (`timeline`)** |
| W0-S2 survey: `(pm)/projects/[projectId]/*` (7) | ✅ done | sonnet, read-only | `.brain/waves/w0-s2-report.md` — 5 EXISTING, **2 NONE (`board`, `timeline`)** |
| W0-S3 survey: API work/structure (15) | ✅ done | sonnet, read-only | `.brain/waves/w0-s3-report.md` — **15/15 EXISTING, 0 new ids needed** |
| W0-S4 survey: API projects/import/repos/scope (13) | ✅ done | sonnet, read-only | `.brain/waves/w0-s4-report.md` — **13/13 EXISTING, 0 new ids** |
| W0-W  allocate ids + write registry rows at 🔜 | ✅ done | controller | FR-063, FR-064, SDD-036 + 2 feature notes |

**Result: 46 routes = 43 EXISTING + 3 NONE → 2 new ids.** The debt was mostly
unwritten annotations, not missing requirements: the service under each route
already declared its FR, and only the route in front of it stayed silent. That
turns Lane G from "design 46 requirements" into "walk an existing trail up one
layer" — mechanical, and checkable.

Verified before relying on it: an FR at 🔜 is excluded from the graph's
`fr_without_code`, so declaring ahead of the annotation does not fail govern.

## Wave 1 — lanes

| Lane | Task | Status | Agent | Runs e2e? |
|---|---|---|---|---|
| **F** fixtures | repay all 3 viewer-fixture files + shrink baseline | ✅ done | sonnet | no |
| **B** enum fix | the 12 **incomplete** enum copies, incl. `KanbanBoard` (FR-063) | ✅ | sonnet | no |

**Lane B outcome, and the part worth remembering.** It repaid 12 copies and found
a *third* instance of the CANCELLED silent loss — the sprint board in
`mode-bodies.jsx` had the same four-column grouping gap. FR-063 and FR-064 are
now ✅, with the board verified in a real browser showing all seven columns.

But one of its repayments had to be reverted, and the fault was the
controller's. The `enum-copy` check counted enum members file-wide, so
`progress-service.js` — which uses three unrelated single-value comparisons
sixty lines apart — was flagged as a copied list. The agent did exactly what the
finding asked and rewrote `status: { not: 'ARCHIVED' }` as
`status: { in: ACTIVE_* }`. Those are not equivalent: statuses are unconstrained
strings in SQLite, so an out-of-enum value would silently drop out of a progress
roll-up.

Reverted, and the check now requires three members inside a five-line window —
26 findings became 10, and all 16 dropped were false positives. Recorded in
[the guard incident](../rca/2026-08-17-a-guard-that-teaches-a-workaround.md) as
its second instance, worse than the first: this one changed a number.

**A new guard's first run is a review of the guard, not a list of defects.**
| **G1** pages | annotate `(pm)` top-level (11) | ✅ | sonnet | no |
| **G2** pages | annotate `(pm)/projects/[projectId]/*` (7) | ✅ | sonnet | no |
| **G3** api | annotate work/structure API (15) | ✅ | haiku | no |
| **G4** api | annotate import/projects/repos API (13) | ✅ | haiku | no |

**Lane G complete: 46/46 routes anchored, `docs/.route-anchor-baseline.json`
emptied.** No lane disagreed with its survey. 52 cited requirement ids, all
declared. Two guard traps worth remembering:

- `preflight` alone still reported all 46 as orphans *after* the annotations
  landed — it reads the committed graph, so it must run **after** `docs:graph`.
  This is the failure CLAUDE.md warns about and it reproduced exactly.
- With the baseline now empty the guard was re-proven both ways: a planted route
  with no `@req` is CRITICAL; removing it returns PASS.
| **T** test-harness | e2e determinism — diagnose before fixing | ✅ | controller | **yes, alone** |

**Lane T outcome.** Root cause measured, not inferred: six workers queueing
behind one Next dev server. On an idle machine six workers take 154s and one
takes 156s — parallelism was buying 1.3% and paying for it in determinism. Fixed
with `workers: 1`; the real gate command now passes under the same synthetic load
that had just failed it.

Holding this lane until the machine was idle turned out to be the whole game: the
flakiness will not reproduce on an idle box, so the first step was to *build* the
load deliberately. Had it run alongside the other five agents it would have
measured itself.

### Why Lane T is held rather than dispatched now

Its first step is *measurement* — does `--workers=1` remove the flakiness — and
the thing being measured is behaviour under machine load. Running it while four
surveys and a test suite occupy the machine would load the box with the very
variable under test, and produce a number that means nothing.

That confound has already overturned two conclusions in this session, both
times because the difference was *when* something ran rather than *what* ran.
Lane T starts when the machine is otherwise idle.

### Why B and G can share the tree

Checked, not assumed: **none of the 12 incomplete enum copies lives in a file on
the route-anchor baseline.** Lane B works in `src/modules/**` and one page
(`projects/new`) that no G lane owns; the G lanes work in `src/app/api/**` and
`(pm)/**/page.jsx`. The overlap is empty, so five agents write concurrently
without a lock.

The complete copies in `(pm)/audit/page.jsx` *are* in both sets — which is
exactly why Lane B is scoped to incomplete copies only. Those are also the
silent-loss class, so the scoping costs nothing.

### Git is single-writer: the controller

Lanes share one working tree, so no lane branches, commits, stashes or pushes.
Agents leave edits in the tree; the controller stages them by path into separate
commits and drives the merge train. A lane that ran `git stash` mid-flight would
take another lane's uncommitted work with it.

## Controller decisions taken during Wave 0

**D1 — a "browse-all" page anchors on the existing requirement, whose text is
sharpened to name the surface.** Raised by S1: `dependencies`, `milestones` and
`work` pages deliver behaviour declared by FR-007 / FR-006 / FR-005, but those
statements describe the data model, not a screen. The alternative was minting
three new FRs.

Rejected, because three near-duplicate ids that nothing else references make the
registry *less* navigable, and the id contract's prohibition is on renumbering
and on reusing an id for a **different** statement — not on stating the same
requirement more completely. FR-014 already names its own UI ("+ UI browser"),
so the registry has the precedent. Same move as FR-059 and SDD-017 earlier
today.

**D3 — Board and Schedule get new ids; S2's recommendation was overridden.**
S2 proposed widening FR-040's text to cover them, consistent with D1. Rejected,
and the line between the two cases is the useful part:

| | FR declares the behaviour? | Move |
|---|---|---|
| browse-all pages (D1) | yes — text just never names the screen | sharpen the statement |
| Board / Schedule | **no requirement anywhere describes a status board or a date grid** | mint a new id |

*Unstated surface for stated behaviour* is an incomplete sentence. *Unstated
behaviour* is a missing requirement. Widening FR-040 would have put new
requirement content under an existing key — the one thing the id contract
forbids. SDD-019 had asserted its tab owned four sub-views while its FR stated
two, so two shipped with nothing behind them; it now cites FR-063/FR-064.

**D4 — a guard that makes people move code to avoid it is fixed, not obeyed.**
Lane F relocated a Membership DB mock into a new fixture file purely to escape
the ratchet's proximity window, and documented the workaround in that file's
header. Both halves are the wrong outcome: the workaround becomes the lesson,
and the signal weakens everywhere else. The heuristic now suppresses a `role:`
that sits on a line carrying Membership-only fields (`personId`,
`domainKeysJson`, `employeeRef`, `branchId`, `tenantId`) — provably DB data, never
a viewer. The mock moved back next to its tests. Proven in both directions: a
planted hand-built viewer is still caught; the Membership rows no longer fire.

**D2 — the controller's own steer was wrong about `/api/scope`.** The brief told
S4 to expect NONE for `/api/scope` and `/api/resolve`. S4 disagreed, said so
explicitly, and was right: `scope-service.js` carries `@req FR-001` and a
FR-020 branch. Verified directly. Recorded because a survey that contradicts its
brief and flags the contradiction is the behaviour worth having.

## Gates

Worker → verify gate (separate agent, never self-review) → **controller runs the
full suite itself on integrated main**. The session record is that seven agents
and two gates reported e2e green while the full suite was red.

## Merge protocol

PRs prepared in parallel, merged **one at a time**. Each rebases and re-runs
`npm run govern` before entering the queue: six generated files are rewritten by
every branch and CI fails if a branch ships a stale one. Conflict resolution
there is mechanical — take theirs, regenerate.
