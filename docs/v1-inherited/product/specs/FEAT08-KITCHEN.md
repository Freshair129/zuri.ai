<!-- ADR-005: imported from Zuri V1 — DO NOT EDIT THIS FILE -->
> **Inherited from Zuri V1 · read-only.**
> Source: `G:/zuri/docs/product/specs/FEAT08-KITCHEN.md` @ `0b6d3c3` · imported 2026-08-11
> This describes **V1 semantics**, where *tenant* means **one shop** — V2's scope
> chain (Portfolio → Tenant → Business → Workspace) is different. Corrections belong
> in a V2 document that cites this file, never in this file. Cite its ids with a
> `V1-` prefix (e.g. `V1-ADR-060`) so they cannot be confused with V2 ids.

# FEAT-KITCHEN — Kitchen Operations Module

**Status:** APPROVED
**Version:** 1.2.0
**Date:** 2026-04-07
**Approved:** 2026-04-07
**Last Amended:** 2026-04-18 (v1.2.0 — add "เมนูอาหาร" tab, ZDEV-TSK-20260418-002)
**Author:** Boss (Product Owner)
**Reviewer:** Claude (Architect)
**Depends On:** FEAT01 (tenantId), FEAT07 (Enrollment → kitchen demand)
**Related:** FEAT06 (POS ingredient usage)

---

## 1. Overview

Kitchen Operations module สำหรับโรงเรียนสอนทำอาหารและธุรกิจ F&B ครอบคลุมการจัดการสูตรอาหาร (Recipe), คลังวัตถุดิบ (Ingredient) แบบ FEFO และการตัดสต็อกอัตโนมัติ

**Core value:** "รู้ก่อนเปิดคลาส — วัตถุดิบพร้อม ต้นทุนโปร่งใส"

---

## 2. Terminology

| คำศัพท์ | ความหมาย |
|---|---|
| **Recipe** | สูตรอาหารที่ผูกกับ Course/Product |
| **Ingredient** | วัตถุดิบหลัก พร้อมหน่วยนับและราคา |
| **Lot** | ล็อตวัตถุดิบที่รับเข้า (มีวันหมดอายุ) |
| **FEFO** | First-Expiry-First-Out — ตัดล็อตที่ใกล้หมดอายุก่อน |
| **Wastage** | ของเสียที่เกิดขึ้นระหว่างการผลิต/คลาส |

---

## 3. Stock Management (FEFO Logic)

ระบบใช้ logic **FEFO (First-Expiry-First-Out)** ในการตัดสต็อก:
1. เมื่อมีการหักสต็อก ระบบจะดึง `IngredientLot` ของวัตถุดิบนั้น
2. เรียงลำดับตาม `expiresAt` จากน้อยไปมาก
3. ตัดยอด `remainingQty` ทีละล็อตจนครบจำนวน
4. อัปเดต `currentStock` (denormalized field) ในตาราง `Ingredient`

---

## 4. API Endpoints

| Method | Path | Action |
|---|---|---|
| `GET` | `/api/kitchen/ingredients` | รายการวัตถุดิบและสต็อกปัจจุบัน |
| `POST` | `/api/kitchen/lots` | รับวัตถุดิบเข้าคลัง (Goods Receipt) |
| `GET` | `/api/kitchen/expiring` | ดูวัตถุดิบที่กำลังจะหมดอายุ |
| `GET` | `/api/products?includeRecipes=true` | รายการเมนูอาหาร (Products) + สูตรที่ผูกไว้ |
| `POST`/`DELETE` | `/api/products/[id]/recipes` | ผูก / ยกเลิก การผูกสูตรอาหารให้เมนู |
| `GET` | `/api/kitchen/recipes` | รายการสูตรอาหารทั้งหมดสำหรับเลือกผูก |

---

## 4.1 Module Tabs (UI)

| Tab | Path | Purpose |
|---|---|---|
| Dashboard | `/kitchen` | สรุปสถานะ: Critical Stock, Upcoming Classes, Recipe Repository |
| สต๊อก | `/kitchen/stock` | คลังวัตถุดิบ + FEFO lots |
| **เมนูอาหาร** *(new v1.2.0)* | `/kitchen/menu` | รายการเมนู (Product) + ผูกสูตรอาหาร (Recipe) ต่อเมนูได้หลายสูตร — ใช้ `RecipeLinker` ร่วมกับ `/pos/products` |
| สูตรอาหาร | `/kitchen/recipes` | CRUD สูตรอาหาร + วัตถุดิบในแต่ละสูตร |
| จัดซื้อ | `/kitchen/procurement` | Purchase Request / Order |

**Note on เมนูอาหาร:** "เมนู" คือ `Product` เดิม (ไม่มีโมเดลใหม่) — ใช้ `ProductRecipe` junction + `linkRecipe`/`unlinkRecipe` ใน `productRepo` ที่มีอยู่แล้ว หน้าจอนี้เป็น read + link view ไม่ทำ CRUD ของ Product (CRUD ยังอยู่ที่ `/pos/products`).

---

## 5. Implementation Progress ✅

- [x] K-P0-1: Ingredient CRUD Repository (`ingredientRepo.js`)
- [x] K-P0-2: IngredientLot model & Goods Receipt logic
- [x] K-P0-4: FEFO deduction logic (`deductFEFO` in repo)
- [x] K-P0-6: `currentStock` denormalized sync helper
- [x] Multi-tenant isolation for all kitchen data

---

*Status: UPDATED 2026-04-07*
