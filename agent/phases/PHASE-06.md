# Phase 06 — Agent Import + Backup

Implement PlanEnvelope:
- JSON validation
- semantic reference validation
- dry-run
- insert/update/conflict preview
- transactional commit
- AuditEvent

Implement:
- snapshot export
- snapshot import preview
- restore confirmation

No arbitrary code execution.

Output:
`.agent/reports/PHASE-06.md`
