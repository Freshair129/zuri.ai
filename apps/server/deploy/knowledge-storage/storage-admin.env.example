# TASK-ZAI-049 provider-admin template.
# Copy this file to a path outside the checkout and fill it with the pinned
# provider's admin values. The compose overlay passes it only to the storage
# container; the web process must use a separate least-privilege access key.
# Do not commit a filled copy or paste its values into chat, logs or receipts.

# MinIO/AIStor-compatible variable names; confirm the selected image's names in
# its pinned release documentation before starting the container.
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
